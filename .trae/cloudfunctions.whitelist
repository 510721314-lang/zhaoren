# 找人帮忙 · 云函数部署白名单（单一来源）
# 用途：.trae/predeploy.ps1 -Audit 用它检测"云函数目录 ↔ 白名单"漂移；
#       不在本清单内但含 index.js 的目录 = 未登记函数（部署会被 predeploy 拒绝）。
# 规则：新增云函数必须先登记本清单（并在 rules.md 第 5 章同步职责说明）。
# 维护：一行一个函数名；# 起始为注释。

# ── 业务函数（rules.md 第 5 章清单）──
user-login
demand-publish
demand-match
partner-apply
partner-action
order-create
order-action
order-timer
payment-mock
safety-report
im-conv
im-send
evaluation-submit
admin-action

# ── 后续批次新增（已在线上运行）──
init-db
home-action
blog-action
admin-web

# 说明：
# - cloudfunctions/_shared/ 为共享代码暂存目录（无 index.js，不可部署，不列入本清单）
# - 调试类临时云函数一律不列入白名单（rules 第 5 章：临时函数需 zz- 前缀 + 守卫 + 登记
#   .trae/zz-registry.md）。历史上本地曾有 admin-test / export-config 两个手工调试函数
#   （已核实从未部署到云端；因含 env 切换与明文导出密钥逻辑，2026-09-23 已删除）。