# 临时云端测试工具与 smoke 入参
.tmp-cloud-runner/
sm*.json

# Trae skills 已入库(团队共享), 仅本地缓存类文件忽略
.trae/cache/

# IDE 临时文件
.idea/
.vscode/
.DS_Store

# 系统临时文件
Thumbs.db
desktop.ini
node_modules/

# 真机预览产物（每次 preview 都变更，不入库）
/preview-qr.png

# 本地备份目录
.backup-*/

# Git Bundle 备份产物（不入仓库，由热备目录留存）
*.bundle
zhaoren_backup_*/

# 临时云函数（手动调试用，不上仓库；云端残留自己 rm）
cloudfunctions/admin-test/
cloudfunctions/export-config/
