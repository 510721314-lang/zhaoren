# 耍伴小程序一期交付说明

> 收尾日期：2026-09-14
> 范围：pages-v2 16 页 + 8 个全局组件 + styles/tokens + utils 工具
> 数据来源：全部走 mock（`miniprogram/mock/*.js`），上线前需切真实接口（见末尾清单）

## 一、页面交付清单

| # | 页面路径 | 完成状态 | 实现的PRD章节号 | 遗留项 |
|---|---|---|---|---|
| P01 | pages-v2/login/login | ✅ 完整 | 3.1.1 / 1.6.1 / 1.5 / R5 | mock 用户身份/年龄/紧急联系人；手机号验证、身份证 OCR、人脸核验均为占位（需对接 API-001/002） |
| P02 | pages-v2/index/index | ✅ 完整 | 3.2.1 / 3.2.3 / 1.8.1 / 1.7 / 1.8.4 | 推荐耍伴列表、新人福利 banner、定位兜底均 mock；城市选择/搜索页为占位 toast |
| P03 | pages-v2/square/square | ✅ 完整 | 2.2 / 1.8.4 / 1.5 | 抢单 R9 校验 mock；供需不足态/智能扩圈/跨城推荐/安抚券未实现（PRD1.8.4 二期） |
| P04 | pages-v2/message/message | ✅ 完整 | 3.4 / 3.5.2 / 3.4.4 | 会话列表、系统通知、客服入口均 mock；IM 长连接未接入 |
| P05 | pages-v2/profile/profile | ✅ 完整 | 1.6.1 / 3.1.2 / 3.11 | 双信用分/订单四宫格/耍伴专区均 mock；订单列表跳转 `/pages/order/order`（旧 v1） |
| P06 | pages-v2/publish/publish | ✅ 完整 | 3.3 / 3.3.1 / 3.5.1-V15 / 1.5 / R1 | 草稿箱/AA 确认弹窗/就医免责声明均 mock；地图选点占位（需对接 wx.chooseLocation） |
| P07 | pages-v2/demand-detail/demand-detail | ✅ 完整 | 2.2 / 3.3 | 推荐耍伴横滑 mock；接单后跳 S1 订单为模拟 |
| P08 | pages-v2/partner-detail/partner-detail | ✅ 完整 | 3.2.3 / 3.1.4 | 资质证书/动态列表/评价列表 mock；<3 单时「数据积累中」已实现 |
| P09 | pages-v2/chat/chat | ✅ 完整 | 3.4 / 3.4.1.1 / 3.4.2 | 四确认进度/模板消息卡/快捷键/客服介入全 mock；IM 长连接未接入 |
| P10 | pages-v2/pay/pay | ✅ 完整 | 3.5.1 / 3.7 / R5 | 费用明细/AA 标注/担保说明完整；支付为 mock（`is_mock=true`），保险同步投保为 toast 模拟 |
| P11 | pages-v2/order-detail/order-detail | ✅ 完整 | 3.5.2 / 3.5.3 / 3.5.4 / 3.5.5 / 3.7 / R1 | 13 态进度轴/改期弹窗/取消梯度退款/保险卡均 mock；改期受 R1 限制已生效 |
| P12 | pages-v2/safety/safety | ✅ 完整 | 3.6 / 3.6.1 / 3.6.2 | 位置共享/定时打卡/分级求助全 mock；地图占位、震动反馈未接入 |
| P13 | pages-v2/evaluate/evaluate | ✅ 完整 | 3.5.7 / 2.1 | 5 星/快捷标签/文字评价/匿名说明/48h 倒计时 mock；评价激励 1 元券 toast 模拟 |
| P14 | pages-v2/workbench/workbench | ✅ 完整 | 3.10 / 3.10.3 / 3.6.3 | 接单开关/周日历/时间轴/资金速览/待办全 mock；一键报警为 toast（需对接 API-012） |
| P15 | pages-v2/accept-config/accept-config | ✅ 完整 | 3.10.1 / R9 / R1 | 场景多选/时段/单价/距离/上限/公益/缓冲全 mock；保存为本地存储 |
| P16 | pages-v2/wallet/wallet | ✅ 完整 | 3.10.3 / 3.5.1 | 余额/极速提现/四态 tab/收益明细/提现弹窗全 mock；银行卡管理/发票/完税/冻结资金为 toast 占位 |

## 二、组件清单

| 组件路径 | 完成状态 | 用途 |
|---|---|---|
| components/nav-bar | ✅ | 通用顶部导航 |
| components/custom-tab-bar（项目根） | ✅ | 自定义 tabBar（用户/耍伴双身份切换） |
| components/night-mask | ✅ | R1 夜间 23:00-06:00 全局遮罩，仅允许「预约明早」 |
| components/empty-state | ✅ | 通用空态/网络异常态（带重试按钮） |
| components/tag-chip | ✅ | 场景/属性标签（含场景色映射，允许枚举色值） |
| components/order-progress | ✅ | 13 态订单进度轴 |
| components/confirm-progress | ✅ | 四确认进度条 |
| components/tm-template-card | ✅ | 聊天模板消息卡 |
| components/partner-card | ✅ | 耍伴推荐横滑卡 |
| components/bottom-sheet | ✅ | 通用底部弹窗 |
| components/safe-sell-bar | ✅ | 首页四大卖点条 |

## 三、自查结果

### 1. 硬编码扫描
- ✅ `pages-v2/**/*.wxss`：0 处硬编码色值（`#xxx`），0 处字号字面量（`font-size: Nrpx`）
- ✅ `components/**/*.wxss`：0 处硬编码色值，0 处字号字面量
- ✅ `styles/tokens.wxss`：仅作为 CSS 变量定义源，色值合法
- ✅ `pages/**/*.wxss`（v1 历史目录）：已批量替换为 tokens 变量引用
- ✅ `custom-tab-bar/index.wxss`：已批量替换为 tokens 变量引用
- 注：`tag-chip` 场景色映射按任务要求保留为枚举色值（合法例外）

### 2. 状态补齐（16 页三态 UI）
| 页面 | 空态 | 加载态 | 网络异常态 | 备注 |
|---|---|---|---|---|
| login | — | — | — | 用户主动操作页，无列表加载 |
| index | ✅ empty-state | ✅ 骨架屏 | ✅ 重试 | |
| square | ✅ empty-state | ✅ 骨架屏 | ✅ 重试 | |
| message | ✅ empty-state | ✅ 骨架屏 | ✅ 重试 | |
| profile | — | — | — | 静态信息页，无列表加载 |
| publish | — | — | — | 表单页，用户主动输入 |
| demand-detail | ✅ empty-state | ✅ 骨架屏 | ✅ 重试 | |
| partner-detail | ✅ empty-state | ✅ 骨架屏 | ✅ 重试 | |
| chat | ✅ empty-state | ✅ 骨架屏 | ✅ 重试 | |
| pay | ✅ empty-state | ✅ 骨架屏 | ✅ 重试 | |
| order-detail | ✅ empty-state | ✅ 骨架屏 | ✅ 重试 | |
| safety | ✅ empty-state | ✅ 骨架屏 | ✅ 重试 | |
| evaluate | ✅ empty-state | ✅ 骨架屏 | ✅ 重试 | |
| workbench | ✅ empty-state | ✅ 骨架屏 | ✅ 重试 | |
| accept-config | ✅ empty-state | ✅ 骨架屏 | ✅ 重试 | |
| wallet | ✅ empty-state | ✅ 骨架屏 | ✅ 重试 | |

合计 13 页完整三态，3 页（login/profile/publish）为用户主动操作或静态信息页，按 UI 规范不强制列表加载三态。

### 3. 红线终检
- ✅ **W3/W7/W9/健身/情绪/宠物场景入口**：全局搜索 `pages-v2` 与 `config/enums.js`，一期仅暴露 5 场景（medical/study/life/travel/online）；「更多场景」灰态点击 toast「健身/情绪/宠物陪伴即将上线」，无任何暴露
- ✅ **night-mask 全局挂载**：已在 16 个 pages-v2 页面 wxml 末尾挂载 `<night-mask visible="{{isRedline}}">`；js onShow 调用 `redline.isInRedline()`；23:00-06:00 命中时遮罩全屏拦截
- ✅ **R1 时间红线**：
  - 发布页时间 picker `start="06:00" end="23:00"`，`onTimeChange` 调 `redline.isServiceTimeAllowed()` 二次校验
  - 订单改期 picker 同上，`onModifyTimeChange` 调 `redline.isServiceTimeAllowed()` 拦截越界
- ✅ **三类人群校验**：
  - **未成年人（<18）**：login.js `afterVerified` 调 `redline.isAdult(age)`，不通过拦截提示 R5
  - **18-22 岁**：publish.js 提交时调 `redline.validateYouthAmount(amount, age)`，超 200 元拦截
  - **60 岁以上**：login.js `afterVerified` 检测 `age >= CONFIG.ELDERLY.age`，toast「长者保护已启用·安全打卡 20 分钟/次」

## 四、真接口替换清单（对照 PRD 第 17 章 API-001~015）

> 全项目所有数据均来自 `miniprogram/mock/*.js`，上线前必须按下表逐项替换为真实 API。

| API 编号 | 接口路径 | 方法 | 当前 mock 文件位置 | 调用方页面 | 替换说明 |
|---|---|---|---|---|---|
| API-001 | /v1/auth/wx-login | POST | mock/users.js → CURRENT_USER | pages-v2/login/login.js | 替换 `afterVerified` 内的用户初始化为 `wx.cloud.callFunction({ name: 'auth', data: { action: 'wx-login', code } })` |
| API-002 | /v1/auth/real-name | POST | mock/users.js → current_user.real_name_verified | pages-v2/login/login.js | 实名认证环节接入身份证 OCR + 人脸核验云函数 |
| API-003 | /v1/auth/partner | POST | mock/users.js → partner_certifications | pages-v2/profile/profile.js（跳 `/pages/partner-apply/partner-apply`） | 耍伴资质审核云函数 |
| API-004 | /v1/demands | POST | mock/demands.js | pages-v2/publish/publish.js | `onPublish` 内 mock push 替换为 `callFunction({ name: 'demand', data: { action: 'create', ...form } })` |
| API-005 | /v1/demands | GET | mock/demands.js → demandList | pages-v2/square/square.js + pages-v2/index/index.js | 列表分页拉取，替换 `buildList(demands)` |
| API-006 | /v1/match | POST | mock/partners.js → recommendPartners | pages-v2/demand-detail/demand-detail.js | 推荐耍伴横滑数据；publish.js 智能派单选项同样接入 |
| API-007 | /v1/orders | POST | mock/orders.js → findOrder/createOrder | pages-v2/demand-detail/demand-detail.js（`onGrab`）+ pages-v2/chat/chat.js（四确认后） | 接单/创建订单云函数 |
| API-008 | /v1/orders/{id}/pay | POST | mock/orders.js + payment-mock | pages-v2/pay/pay.js | 替换 `is_mock=true` 为真实微信支付：服务端统一下单 → `wx.requestPayment` → 回调验签 → 置 S2 |
| API-009 | /v1/orders/{id}/split | GET | mock/orders.js → split | pages-v2/wallet/wallet.js + pages-v2/order-detail/order-detail.js | 分账记录查询 |
| API-010 | /v1/withdraw | POST | mock/wallet.js → withdraw | pages-v2/wallet/wallet.js | `confirmWithdraw` 内 mock 替换为提现申请云函数 |
| API-011 | /v1/safety/report | POST | mock/safety.js → checkin | pages-v2/safety/safety.js | GPS 打卡 `onManualCheckin` 替换 |
| API-012 | /v1/safety/sos | POST | mock/safety.js → sos | pages-v2/safety/safety.js + pages-v2/workbench/workbench.js | 一键求助/报警 `confirmAlarm` 替换 |
| API-013 | /v1/insurance/policy | POST | mock/orders.js → insurance | pages-v2/pay/pay.js（同步投保）+ pages-v2/order-detail/order-detail.js（保单查看） | 支付成功后服务端自动投保 |
| API-014 | /v1/credit/score | GET | mock/users.js → credit_score | pages-v2/profile/profile.js + pages-v2/partner-detail/partner-detail.js | 双信用分卡/耍伴信用分行 |
| API-015 | /v1/orders/{id}/evaluate | POST | mock/evaluations.js | pages-v2/evaluate/evaluate.js | `onSubmit` 替换为评价提交云函数；含 msgSecCheck 内容安全 |

### 补充接口（PRD 17 章外但必须替换）

| 用途 | 当前 mock 位置 | 调用方 | 替换说明 |
|---|---|---|---|
| IM 长连接 | mock/conversations.js + mock/messages.js | pages-v2/message + pages-v2/chat | 接入微信 IM 或自建 WebSocket |
| 订单列表 | mock/orders.js → findOrder | pages-v2/profile（订单四宫格跳 `/pages/order/order`） | 新建订单列表云函数 |
| 订单改期 | mock/orders.js → modifyTime | pages-v2/order-detail `confirmModify` | 改期云函数（含 R1/规则校验） |
| 订单取消 | mock/orders.js → cancel | pages-v2/order-detail `confirmCancel` | 取消云函数（含梯度退款） |
| 加时申请 | mock/orders.js → addTime | pages-v2/order-detail `onAddTime` | 加时云函数 |
| 履约开始/完成 | mock/orders.js → startService/finishService | pages-v2/order-detail | 状态机流转云函数 |
| 内容安全（msgSecCheck/imgSecCheck） | 无 | pages-v2/publish + evaluate + chat + mine（头像） | 上线前必须接入 |
| 紧急联系人确认 | mock/users.js → emergency_contacts | pages-v2/login + pages-v2/index | 短信验证云函数 |
| 接单配置保存 | mock/accept-config.js | pages-v2/accept-config `onSave` | 配置存储云函数 |
| 草稿箱 | mock/drafts.js | pages-v2/publish | 草稿存储云函数 |
| 通知/小红点 | mock/notifications.js | pages-v2/index + pages-v2/message | 系统通知云函数 |

## 五、编译验证

- ✅ 全项目 wxss grep `font-size: Nrpx`：0 处
- ✅ 全项目 wxss grep `#xxx` 散落色值：0 处（tokens.wxss 内变量定义除外）
- ✅ pages-v2 全部 16 页 wxml 含 `night-mask` 和 `isRedline`
- ⚠️ 开发者工具编译 0 error 0 warning：因开发者工具不可在 CI 中调用，需用户在本地 IDE 验证；本次仅做静态扫描验证。预期 warning 可能来自：
  - 旧 `pages/` 目录的 v1 页面（已注册在 app.json 但部分页面 wxml 缺失或语法陈旧）—— 上线前建议从 app.json `pages` 数组移除已废弃的 pages/ 路径
  - `__usePrivacyCheck__` 开关（项目记忆中已确认无意义但保留）

## 六、上线前遗留项（项目记忆汇总）

1. **隐私指引未彻底解决**：后台位置信息声明审核通过 + 真机验证 wx.getLocation 官方弹窗链路
2. **真实支付**：替换 payment-mock 上线，接入微信支付完整链路（服务端统一下单 → wx.requestPayment → 回调验签 → 回调内事务置 S2）
3. **云函数超时**：控制台统一调整或升级套餐（当前锁死 3 秒）
4. **order-timer 超时流转自驱链**：测试环境重建并完成 all_pass 验收
5. **编译全链路实测**：发布 → 匹配 → 四确认 → 支付 → 履约 → 评价 → 打赏 尚未端到端跑通
6. **自测模式清理**：商用前删除 `utils/testmode.js` + `mine.js` 长按版本号入口
7. **法律文档配置**：用户协议、隐私政策、个人信息收集清单（小程序后台）
8. **管理员白名单**：`admin-action` 云函数需添加管理员 openid 白名单
9. **数据库索引**：demand/order_main 集合索引需手动创建
10. **内容安全**：msgSecCheck（昵称/评价/备注/IM）+ imgSecCheck（头像/聊天图/安全报备图）必须接入

---

**交付结论**：一期 16 页 UI 与红线合规已全部完成；所有数据走 mock，上线前需按「真接口替换清单」逐项切换为云函数。建议先在测试环境跑通端到端链路再切换真实支付。
