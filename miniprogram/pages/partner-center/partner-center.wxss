.page-center { padding: 24rpx 24rpx calc(env(safe-area-inset-bottom) + 40rpx); }

/* 接单开关 */
.switch-card {
  display: flex; justify-content: space-between; align-items: center;
  padding: 32rpx; border-radius: 16rpx; margin-bottom: 24rpx;
}
.switch-card.on { background: linear-gradient(135deg, var(--func-success) 0%, var(--func-success) 100%); color: var(--text-invert); }
.switch-card.off { background: linear-gradient(135deg, var(--text-4) 0%, var(--text-3) 100%); color: var(--text-invert); }
.switch-status { font-size: var(--font-display); font-weight: 600; }
.switch-tip { font-size: var(--font-base); opacity: 0.8; margin-top: 4rpx; }

/* 统计 */
.stats-card { margin-bottom: 24rpx; }
.stats-grid { display: flex; margin-top: 16rpx; }
.stat-item { flex: 1; text-align: center; }
.stat-num { font-size: var(--font-display-lg); font-weight: 600; color: var(--primary); }
.stat-label { font-size: var(--font-sm); color: var(--text-2); margin-top: 4rpx; }

/* 时薪 */
.rate-display {
  font-size: var(--font-display-xl); font-weight: 600; color: var(--primary);
  text-align: center; margin: 24rpx 0;
}
.rate-display-sm {
  font-size: var(--font-xxl); font-weight: 600; color: var(--primary);
  margin: 8rpx 0;
}

/* 场景列表(中心页) */
.scene-list-center { margin-top: 16rpx; }
.scene-row {
  border: 2rpx solid var(--border);
  border-radius: 12rpx;
  margin-bottom: 16rpx;
  overflow: hidden;
}
.scene-row-header {
  display: flex; align-items: center;
  padding: 24rpx; background: var(--bg-page);
}
.scene-check {
  width: 36rpx; height: 36rpx; border-radius: 8rpx;
  border: 2rpx solid var(--border); margin-right: 16rpx;
  display: flex; align-items: center; justify-content: center;
  font-size: var(--font-base); color: var(--text-invert);
}
.scene-check.checked { background: var(--primary); border-color: var(--primary); }
.scene-icon { font-size: var(--font-display); margin-right: 12rpx; }
.scene-name { font-size: var(--font-xl); color: var(--text-1); }
.scene-rate-row { padding: 16rpx 24rpx; border-top: 2rpx solid var(--border); }
.scene-rate-row slider { margin: 8rpx 0 0; }

/* 场景网格(旧,保留兼容) */
.scene-grid { display: flex; flex-wrap: wrap; gap: 16rpx; margin-top: 16rpx; }
.scene-chip {
  padding: 16rpx 24rpx; border-radius: 12rpx;
  border: 2rpx solid var(--border); background: var(--bg-page);
  display: flex; align-items: center; font-size: var(--font-md);
}
.scene-chip.active { background: var(--brand-accent-light); border-color: var(--primary); color: var(--primary); }
.chip-icon { margin-right: 8rpx; }

button.btn-block { margin-top: 16rpx; }
