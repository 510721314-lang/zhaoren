/* pages/blog-publish/blog-publish.wxss */
.page-publish { min-height: 100vh; background: var(--bg-page, #F5F5F5); padding: 20rpx 24rpx; }
.card { background: var(--text-invert); border-radius: 20rpx; padding: 28rpx; margin-bottom: 20rpx; }

.content-input { width: 100%; min-height: 220rpx; font-size: var(--font-lg); line-height: 1.7; color: var(--text-1, #1F2937); box-sizing: border-box; }
.word-count { text-align: right; font-size: var(--font-sm); color: var(--text-3, #9CA3AF); margin-top: 12rpx; }

.card-label { font-size: var(--font-md); color: var(--text-2, #4B5563); margin-bottom: 20rpx; }
.scene-pick { display: flex; flex-wrap: wrap; }
.scene-chip {
  padding: 12rpx 26rpx; margin: 0 18rpx 18rpx 0;
  font-size: var(--font-base); color: var(--text-2, #4B5563); background: var(--bg-page);
  border-radius: 28rpx; position: relative;
  transition: var(--transition, 0.2s ease);
}
.scene-chip:active { transform: scale(0.95); }
.chip-on {
  background: var(--primary, #D4875A); color: var(--text-invert);
  padding-right: 50rpx;
}
.chip-on::after {
  content: '✓'; position: absolute; right: 18rpx; top: 50%;
  transform: translateY(-50%); font-size: var(--font-sm);
}

/* 图片九宫格 */
.img-picker { display: flex; flex-wrap: wrap; }
.picker-item { position: relative; width: 200rpx; height: 200rpx; margin: 0 18rpx 18rpx 0; }
.picker-img { width: 200rpx; height: 200rpx; border-radius: 12rpx; background: var(--bg-page); }
.up-mask {
  position: absolute; inset: 0; background: rgba(0,0,0,0.45);
  display: flex; align-items: center; justify-content: center;
  border-radius: 12rpx;
}
.up-spinner {
  width: 40rpx; height: 40rpx;
  border: 5rpx solid rgba(255,255,255,0.3);
  border-top-color: #fff;
  border-radius: 50%;
  animation: pub-spin 0.8s linear infinite;
}
@keyframes pub-spin { to { transform: rotate(360deg); } }
.up-del { position: absolute; top: -14rpx; right: -14rpx; width: 40rpx; height: 40rpx; line-height: 36rpx; text-align: center; background: rgba(0,0,0,0.6); color: var(--text-invert); border-radius: 50%; font-size: var(--font-lg); }
.picker-add {
  width: 200rpx; height: 200rpx; border: 2rpx dashed var(--border, #D1D5DB);
  border-radius: 12rpx; display: flex; flex-direction: column;
  align-items: center; justify-content: center; color: var(--text-3, #9CA3AF);
  transition: var(--transition, 0.2s ease);
}
.picker-add:active { background: var(--bg-page); transform: scale(0.97); }
.add-plus { font-size: var(--font-2xl); line-height: 1; }
.add-text { font-size: var(--font-sm); margin-top: 10rpx; }

.submit-btn { margin-top: 12rpx; }
.tip { text-align: center; font-size: var(--font-sm); color: var(--text-3, #9CA3AF); margin-top: 24rpx; line-height: 1.6; }
