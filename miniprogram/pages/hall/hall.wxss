.page-hall { padding: 24rpx; }

/* 空状态已提升为全局类(app.wxss .empty-state) */

/* 需求卡片 */
.demand-card {
  margin-bottom: 24rpx; padding: 28rpx;
  transition: var(--transition, 0.2s ease);
}
.demand-card:active { transform: scale(0.99); }
.demand-head { display: flex; justify-content: space-between; align-items: center; margin-bottom: 16rpx; }
.scene-tag {
  display: inline-block; padding: 6rpx 20rpx;
  background: rgba(212,135,90,0.12); color: var(--primary);
  border-radius: 20rpx; font-size: var(--font-base); font-weight: 500;
}
.demand-no { font-size: var(--font-sm); color: var(--text-3); }

.demand-content { font-size: var(--font-xl); color: var(--text-1); font-weight: 500; line-height: 1.5; }
.demand-remark { font-size: var(--font-md); color: var(--text-3); margin-top: 8rpx; line-height: 1.5; }

.demand-meta { margin-top: 20rpx; }
.meta-row { display: flex; align-items: center; padding: 6rpx 0; }
.meta-label { font-size: var(--font-md); margin-right: 12rpx; }
.meta-val { font-size: var(--font-md); color: var(--text-2); }

.aa-tag {
  display: inline-block; margin-top: 16rpx;
  padding: 4rpx 16rpx; background: var(--bg-page);
  color: var(--warning); border-radius: 8rpx; font-size: var(--font-sm);
}

.demand-foot { display: flex; justify-content: space-between; align-items: center; margin-top: 24rpx; }
.ago { font-size: var(--font-sm); color: var(--text-3); }
.take-btn { font-size: var(--font-md); padding: 0 28rpx; line-height: 60rpx; }

/* 发单人模式: 发布引导卡 */
.user-hall-hero {
  padding: 48rpx 28rpx; text-align: center;
  background: linear-gradient(135deg, var(--banner-grad-start) 0%, var(--text-invert) 60%);
}
.hero-title { font-size: var(--font-xxl); font-weight: 600; color: var(--text-1); }
.hero-sub { font-size: var(--font-md); color: var(--text-2); line-height: 1.6; margin: 16rpx 0 28rpx; }
.hero-btn { margin-bottom: 20rpx; }
.hero-switch {
  font-size: var(--font-base); color: var(--primary);
  transition: var(--transition, 0.2s ease);
}
.hero-switch:active { opacity: 0.7; }
.hall-section {
  font-size: var(--font-lg); font-weight: 600; color: var(--text-1);
  margin: 8rpx 4rpx 16rpx; padding-left: 16rpx; position: relative;
}
.hall-section::before {
  content: ''; position: absolute; left: 0; top: 50%; transform: translateY(-50%);
  width: 6rpx; height: 28rpx; border-radius: 3rpx;
  background: var(--primary, #D4875A);
}
.hall-section-gap { margin-top: 32rpx; }
.take-link {
  font-size: var(--font-base); color: var(--primary);
  transition: var(--transition, 0.2s ease);
}
.take-link:active { opacity: 0.7; }

/* 我接的单 */
.empty-line { font-size: var(--font-md); color: var(--text-3); text-align: center; padding: 20rpx 0 28rpx; }
.myorder-card {
  padding: 24rpx 28rpx; margin-bottom: 16rpx;
  transition: var(--transition, 0.2s ease);
}
.myorder-card:active { transform: scale(0.99); }
.myorder-head { display: flex; justify-content: space-between; align-items: center; margin-bottom: 12rpx; }
.myorder-content { font-size: var(--font-lg); color: var(--text-1); font-weight: 500; }
.myorder-meta { display: flex; flex-direction: column; gap: 6rpx; margin-top: 12rpx; font-size: var(--font-base); color: var(--text-2); }
.myorder-foot { display: flex; justify-content: space-between; align-items: center; margin-top: 14rpx; }
.mo-price { font-size: var(--font-lg); font-weight: 600; color: var(--primary); }
.mo-status { font-size: var(--font-sm); padding: 4rpx 16rpx; border-radius: 8rpx; background: var(--bg-page); color: var(--text-3); }
.mo-status-warn { background: var(--banner-grad-start); color: var(--warning); }
.mo-status-danger { background: var(--func-danger-light); color: var(--danger); }
.mo-status-success { background: var(--func-success-light); color: var(--success); }
.mo-status-primary { background: rgba(212,135,90,0.12); color: var(--primary); }
.mo-status-muted { background: var(--bg-page); color: var(--text-3); }
