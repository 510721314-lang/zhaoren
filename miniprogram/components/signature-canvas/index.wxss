.sgc { position: relative; }
.sgc__canvas {
  width: 100%;
  height: 320rpx;
  background: var(--bg-card);
  border: 1rpx dashed var(--border-ghost);
  border-radius: var(--r-sm);
  box-sizing: border-box;
}
.sgc__hint {
  position: absolute;
  left: 0;
  right: 0;
  top: 50%;
  transform: translateY(-50%);
  text-align: center;
  color: var(--text-disabled);
  font-size: 28rpx;
  pointer-events: none;
}