.sellbar {
  height: 60rpx;
  background: var(--func-success-light);
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 0 12rpx;
}
.sellbar__item {
  font-size: var(--font-sm);
  color: var(--func-success);
  font-weight: 600;
  white-space: nowrap;
  margin: 0 10rpx;
}
